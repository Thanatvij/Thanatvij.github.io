{
  "name": "thanatvij-portfolio-astro",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Astro pilot for the portfolio at https://thanatvij.github.io (home page only)",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "preview:merged": "node scripts/serve-merged.mjs",
    "test": "playwright test",
    "test:update": "playwright test tests/visual.spec.ts --update-snapshots",
    "test:install": "playwright install chromium"
  },
  "dependencies": {
    "astro": "^7.3.3",
    "gsap": "^3.15.0",
    "lenis": "^1.3.26"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0"
  }
}
